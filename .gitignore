node_modules/
dist/
.turbo/
*.log
.DS_Store
coverage/
.env
.env.*

# TypeScript incremental build state
*.tsbuildinfo

# Docusaurus
.docusaurus/
docs/build/

# Raw per-package benchmark output. The committed artefact is
# bench/baseline.json, written by `pnpm bench:baseline`.
bench-report.json

# Scratch probe scripts. Reviewers and contributors write these to check a
# claim against the real code, and they should never reach a commit.
.probe*

# Vite / vitest caches
.vite/
**/node_modules/.vite/
**/node_modules/.vitest/
vite.config.*.timestamp-*
