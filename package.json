{
  "name": "dagr-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for Dagr: directed graph layout, WebGPU rendering, and visual DSLs.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/prnt-design/dagr"
  },
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r --if-present test",
    "bench": "pnpm -r --if-present bench",
    "build": "pnpm -r --if-present build",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@types/node": "^26.1.1",
    "eslint": "^9.39.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.65.0",
    "vitest": "^4.1.10"
  }
}
