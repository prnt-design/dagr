{
  "name": "@dagr/bench",
  "version": "0.0.0",
  "private": true,
  "description": "Benchmark harness for Dagr: shared corpora, the control workload, and the local baseline gate. Never published.",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/prnt-design/dagr.git",
    "directory": "bench"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}
